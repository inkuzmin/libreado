// Generated by CoffeeScript 1.7.1
(function() {
  var Invite, User, config, invite, mongoose, nodemailer, request, should, user;

  request = require("request");

  should = require("chai").should();

  user = require("../models/user");

  invite = require("../models/invite");

  config = {
    maxUsers: 1000,
    mail: "invite@libreado.com"
  };

  mongoose = require("mongoose");

  nodemailer = require("nodemailer");

  mongoose.connect("mongodb://localhost/ololo", function(err) {
    return console.log(err);
  });

  User = user(config, mongoose);

  Invite = invite(config, mongoose, nodemailer, User.model);

  invite = {
    _id: "test",
    email: "inkuzmin@ya.ru"
  };

  describe("Redirections", function() {
    return describe("Open http://libreado.com:8080", function() {
      return it("should return at 200 response code", function(done) {
        User.createUser("testlol");
        return done();
      });
    });
  });

}).call(this);

//# sourceMappingURL=db.map
